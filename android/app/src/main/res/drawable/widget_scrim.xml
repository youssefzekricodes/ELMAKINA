<?xml version="1.0" encoding="utf-8"?>
<!-- A light, even veil over the art so the number and the nudge read against the busy middle of
     the banner (the map and the wall) without the picture going dark. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
  <corners android:radius="@dimen/widget_radius" />
  <solid android:color="#5C0E0B08" />
</shape>
