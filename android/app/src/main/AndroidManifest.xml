<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Google sign-in comes back here. The OAuth round trip runs in a Custom Tab (Google
                 refuses to sign in inside a WebView), and Supabase redirects to this scheme when it
                 is done; singleTask above means it lands on the running activity as appUrlOpen
                 (client/src/lib/social.ts) rather than starting a second copy of the app. The exact
                 URL, com.elmekina.game://auth/callback, must be on the Supabase redirect allow-list. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="com.elmekina.game" android:host="auth" />
            </intent-filter>
        </activity>

        <!-- AdMob. The APPLICATION id, not an ad unit — the SDK reads it from here and refuses to
             initialise without it, failing every ad request. Kept in sync with ADMOB_APP_ID.android
             in client/src/lib/google.ts; the ad UNIT ids are separate and live in .env. -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-4626982618627963~6035406752"
        />
        <!-- Google's own remedy for the SDK doing too much on the main thread at startup. This is a
             game with a 12s turn clock: a stutter during a hand is worse than a slower first ad. -->
        <meta-data android:name="com.google.android.gms.ads.flag.OPTIMIZE_INITIALIZATION" android:value="true" />
        <meta-data android:name="com.google.android.gms.ads.flag.OPTIMIZE_AD_LOADING" android:value="true" />

        <!-- The home-screen streak widget (StreakWidget.java). -->
        <receiver android:name=".StreakWidget" android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/streak_widget_info" />
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
